/**
 * Locks utility - static placeholder file to prevent 404 errors
 * This file is served as a static asset to resolve browser requests for src/lib/locks.ts
 */

// This is a static file served from the public directory
// If you need actual TypeScript functionality, use the version in src/lib/locks.ts
export const locks = {
  // Placeholder for any lock-related utilities
};

export default locks;
